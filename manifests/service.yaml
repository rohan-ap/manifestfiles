apiVersion: v1
kind: Service
metadata:
  name: productpage-dev
  namespace: war-project
spec:
  selector:
    app: productpage
    version: dev
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
