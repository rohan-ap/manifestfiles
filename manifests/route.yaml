apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: productpage-dev-route1
spec:
  to:
    kind: Service
    name: productpage-dev
  port:
    targetPort: 8080
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
